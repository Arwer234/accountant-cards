## Technical stack

-   react-router-dom - provides routing to the app
-   @tanstack/react-query - provides infinite query loading and cache management
-   styled-component - provides CSS-in-JS styling

### How to run

-   Use `npm install` or `yarn` to install dependencies
-   Use `npm start` or `yarn start` to run the app on localhost
