type AccountantCardParagraphProps = {
	subheaderText?: string;
	subheaderSize?: number;
	contentType: 'email' | 'text' | 'amount' | 'header';
	children: string;
};

export type { AccountantCardParagraphProps };
